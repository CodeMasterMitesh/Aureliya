// Vitest setup file for React Testing Library matchers and JSDOM tweaks
import '@testing-library/jest-dom';

// Optional: silence React Router warnings in tests if any
// globalThis.IS_REACT_ACT_ENVIRONMENT = true;
