// Root-level dashboard route mapping to existing admin dashboard implementation.
// This simply re-exports the admin dashboard page so both /dashboard and legacy /admin/dashboard work.
import AdminDashboard from './admin/dashboard'

export default AdminDashboard
